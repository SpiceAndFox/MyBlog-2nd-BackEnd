// No gateway-specific wire assumptions. Models without an exception use the
// generic defaults and do not receive inferred reasoning controls.
module.exports = { id: "generic", defaults: {}, models: [] };
